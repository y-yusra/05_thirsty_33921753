<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search - <%= shopData.shopName %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Search <%= shopData.shopName %></h1>
    
    <form action="/search_result" method="GET">
        <div>
            <label for="search_text">Search by name:</label>
            <input id="search_text" type="text" name="search_text" placeholder="Enter drink name...">
        </div>
        
        <div>
            <label for="category">Filter by category:</label>
            <select id="category" name="category">
                <option value="all">All Categories</option>
                <% shopData.productCategories.forEach(category => { %>
                    <option value="<%= category %>"><%= category %></option>
                <% }); %>
            </select>
        </div>
        
        <input type="submit" value="Search">
    </form>
    <nav>
        <a href="/">Home</a> | 
        <a href="/about">About</a> | 
        <a href="/search">Search</a> | 
        <a href="/register">Register</a> |
        <a href="/survey">Survey</a>
    </nav>
</body>
</html>